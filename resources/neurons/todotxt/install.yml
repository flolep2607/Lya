- name: Kalliope todotxt neuron install
  hosts: localhost
  gather_facts: no
  connection: local
  become: true

  tasks:
    - name: "Install pip dependencies"
      debug:
          msg: "I do nothing at all"
